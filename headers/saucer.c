#include "saucer.h"
#include "trianglecoli.h"
#include "structure.h"
void display(saucer *S){
    switch(S->Type){
        case long_range:
        //if ! collide with bullet display, if collide then remove
        
        case short_range:
        //if ! collide with bullet display, if collide then remove

        case medium_range:
        //if ! collide with bullet display, if collide then remove
        
        case mini_boss:
        //if ! collide with bullet display, if collide then remove
    }
}